<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.SetmealMapper">

    <select id="list" parameterType="com.sky.entity.Setmeal" resultType="com.sky.entity.Setmeal">
        select * from setmeal
        <where>
            <if test="name != null">
                and name like concat('%',#{name},'%')
            </if>
            <if test="categoryId != null">
                and category_id = #{categoryId}
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
        </where>
    </select>

    <select id="getDishItemById" parameterType="java.lang.Long" resultType="com.sky.vo.DishItemVO">
        select sd.copies, d.description, d.image, sd.name
        from setmeal_dish sd left join dish d on sd.dish_id = d.id
        where sd.setmeal_id = #{id}
    </select>

    <select id="pageQuery" parameterType="com.sky.dto.SetmealPageQueryDTO" resultType="com.sky.entity.Setmeal">
        select * from setmeal
        <where>
            <if test="name != null">
                and name like concat('%', #{name}, '%')
            </if>
            <if test="categoryId != null">
                and category_id = #{categoryId}
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
        </where>
        order by create_time desc
    </select>

    <select id="getById" parameterType="java.lang.Long" resultType="com.sky.entity.Setmeal">
        select * from setmeal where id = #{id}
    </select>
</mapper>